// TODO Phase 8 (Task 29): replace with the full Ollama HTTP configuration form.
import type { AdapterViewProps } from '../../../../core/adapters/AdapterViewRegistry'

export function OllamaHttpView(_props: AdapterViewProps) {
  // Full implementation in Phase 8 Task 29.
  return (
    <div className="p-4 text-sm text-white/60">
      Ollama HTTP configuration (fields pending)
    </div>
  )
}
