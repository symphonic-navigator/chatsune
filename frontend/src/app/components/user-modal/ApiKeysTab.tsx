// TODO Phase 9: rewrite to manage web-search credentials and delegate
// LLM credentials to the new "LLM Providers" tab (Phase 8 Task 26).

export function ApiKeysTab(_props: { onProvidersLoaded?: () => void }) {
  return (
    <div className="p-6 text-white/60">
      API-Keys-Tab wird überarbeitet (Phase 9)
    </div>
  )
}
