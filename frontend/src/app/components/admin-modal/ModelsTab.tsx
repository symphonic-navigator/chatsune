// TODO Phase 9: admin model curation is being removed (Task 32).
// Placeholder to keep the AdminModal tab compile-safe until then.

export function ModelsTab() {
  return (
    <div className="p-6 text-white/60">
      Admin-Modelle wird überarbeitet (Phase 9)
    </div>
  )
}
